{
  "name": "rete-node-groups-plugin",
  "version": "0.1.0",
  "description": "A plugin for the the rete editor which enables bundling multiple nodes into one node group",
  "scripts": {
    "build": "tsup ./src/index.ts --dts --format esm",
    "lint": "rete lint"
  },
  "author": "Timo Lehnertz",
  "license": "MIT",
  "peerDependencies": {
    "rete": "^2.0.0"
  },
  "devDependencies": {
    "rete-cli": "^1.0.3",
    "tsup": "^8.2.3",
    "typescript": "4.8.4"
  },
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "dependencies": {
  }
}
